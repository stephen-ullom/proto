{
  "name": "proto",
  "version": "1.0.0",
  "description": "Generate application prototypes with hyperspeed.",
  "author": "Stephen Ullom",
  "bin": {
    "proto": "dist/scripts/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist/* && tsc",
    "pack": "npm run build && npm pack"
  },
  "dependencies": {
    "typescript": "^5.0.4",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^18.16.12",
    "@types/ws": "^8.5.4"
  }
}
